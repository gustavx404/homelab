---
name: CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  yamllint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ibiqlik/action-yamllint@v3
        with:
          file_or_dir: compose/ homeassistant/ suricata/ monitoring/ crowdsec/ traefik/ frigate/ homepage/

  compose-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate all stacks
        run: docker compose -f compose/compose.yaml config --quiet

  config-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Scan compose for misconfigurations
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: config
          scan-ref: compose/
          severity: CRITICAL,HIGH
          exit-code: 1

  image-scan:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        image:
          - ghcr.io/home-assistant/home-assistant:stable
          - ghcr.io/esphome/esphome:stable
          - grafana/grafana:11.6.0
          - traefik:v3.3
          - prom/prometheus:v3.4.0
          - codeberg.org/forgejo/forgejo:16.0.2
          - crowdsecurity/crowdsec:latest
          - ghcr.io/blakeblackshear/frigate:stable
          - ghcr.io/immich-app/immich-server:v3.1.0
          - ghcr.io/immich-app/immich-machine-learning:v3.1.0
          - valkey/valkey:9
          - ghcr.io/immich-app/postgres:14-vectorchord0.4.3-pgvectors0.2.0
    steps:
      - uses: actions/checkout@v4
      - name: Scan ${{ matrix.image }}
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: image
          image-ref: ${{ matrix.image }}
          severity: CRITICAL
          exit-code: 0
          ignore-unfixed: true
