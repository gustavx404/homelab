# CrowdSec database config — MariaDB instead of SQLite
# Database and user created in mariadb container

db_config:
  type: mysql
  user: crowdsec
  password: crowdsec_db_pass_2026
  db_name: crowdsec
  host: mariadb
  port: 3306
  flush:
    max_items: 5000
    max_age: 7d
